---
title: "Coding agents build from your docs. Nobody checks your docs."
description: "The instruction file in the official MCP C# SDK tells every coding agent to use a class the code no longer has. Copilot wrote the instruction; Copilot removed the class. That is what happens now that documentation is an input to code generation and nothing compares it to the code."
date: 2026-09-24
category: "Data & research"
cover: "stale-line"
---

Every coding agent starts the same way. Before it writes a line, it reads the repository's instruction file: `CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`. Whatever that file says about the code, the agent takes as fact and builds on. It does not verify; it has no way to. And nothing else does either. Your tests check the code. Your types check the code. Nobody checks the document that the code is now generated from.

Here is what that looks like in the [official C# SDK for the Model Context Protocol](https://github.com/modelcontextprotocol/csharp-sdk), the protocol built to give AI models accurate context. Line 258 of its Copilot instructions:

<div class="bp-figure" data-reveal>
<div class="bp-ghp">
<div class="bp-ghp-bar"><span class="bp-ghp-crumbs">csharp-sdk <i>/</i> .github <i>/</i> <b>copilot-instructions.md</b></span><span class="bp-ghp-tabs"><span class="is-on">Preview</span><span>Code</span><span>Blame</span></span></div>
<div class="bp-ghp-body">
<p class="bp-ghp-h">Server Implementation</p>
<ul>
<li><span class="bp-ghp-ln">256</span><span class="bp-ghp-text">Server primitives (tools, prompts, resources) are discovered via reflection using attributes</span></li>
<li><span class="bp-ghp-ln">257</span><span class="bp-ghp-text">Support both attribute-based registration (<code>WithTools&lt;T&gt;()</code>) and instance-based (<code>WithTools(target)</code>)</span></li>
<li class="is-hl"><span class="bp-ghp-ln">258</span><span class="bp-ghp-text">Use <mark>McpServerFactory</mark> to create server instances with configured options<br /><span class="bp-ghp-callout"><b>Not in the code.</b> Deleted 2 Dec 2025 in <a href="https://github.com/modelcontextprotocol/csharp-sdk/pull/985">#985</a>.</span></span></li>
</ul>
</div>
</div>
<p class="bp-figure-caption"><a href="https://github.com/modelcontextprotocol/csharp-sdk/blob/main/.github/copilot-instructions.md#L258">The line on <code>main</code></a>. This file exists to brief a coding agent before it writes anything.</p>
</div>

`McpServerFactory` does not exist. It was deleted last December. Search the repository for the name and you get one result: this line, in the file every agent reads first.

<div class="bp-callout">
Copilot wrote that instruction. Seven weeks later, Copilot deleted the class it names. Ten months on, every agent that opens the repository is still told to use it.
</div>

The person who wrote a design doc in 2015 knew a colleague might read it, get confused, and ask. The agent reading line 258 today does not ask. It writes `McpServerFactory`, watches the build fail, and goes looking for what it should have done. That one is cheap: a few wasted turns. The reason to care is what it tells you about every other sentence in that file, and in yours.

## How the input to your codebase went wrong

None of the people involved would have caught this, because nothing they did required them to:

<div class="bp-figure" data-reveal>
<p class="bp-figure-title">One line, five commits</p>
<ol class="bp-timeline">
<li><span class="bp-tl-date">16 Sep 2025</span><span class="bp-tl-body"><code>McpServerFactory</code> is marked <code>[Obsolete]</code>: <em>&ldquo;Use McpServer.Create instead. This member will be removed in a subsequent release.&rdquo;</em> <a href="https://github.com/modelcontextprotocol/csharp-sdk/commit/38b4a269">38b4a26</a></span></li>
<li><span class="bp-tl-date">13 Oct 2025</span><span class="bp-tl-body">Copilot opens <a href="https://github.com/modelcontextprotocol/csharp-sdk/pull/858">#858</a>, &ldquo;Set up Copilot instructions for repository&rdquo;, a long and mostly accurate briefing. A maintainer reviews and merges it. The instruction to use <code>McpServerFactory</code> is line 222.</span></li>
<li class="is-del"><span class="bp-tl-date">2 Dec 2025</span><span class="bp-tl-body"><a href="https://github.com/modelcontextprotocol/csharp-sdk/pull/985">#985</a>, &ldquo;Remove obsolete APIs from codebase&rdquo;, deletes <code>McpServerFactory.cs</code>. Authored by Copilot, co-authored by three of the project&rsquo;s developers. The instructions file is not in the diff, so nobody reviewing the removal opened it.</span></li>
<li><span class="bp-tl-date">Apr&ndash;Aug 2026</span><span class="bp-tl-body">Four more commits edit the instructions file. None touches the line, which drifts down to 258.</span></li>
<li class="is-now"><span class="bp-tl-date">Today</span><span class="bp-tl-body">Line 258 is still there.</span></li>
</ol>
</div>

The reviewer of the instructions read a long document and it was almost entirely right. The reviewer of the removal read a diff and the document was not in it. That is the whole mechanism. A document and the code it describes live in different files, and a pull request only ever shows you one of them.

## The agent doesn't just read the wrong sentence. It repeats it.

In April 2026 a commit authored as "Architecture Bot" [added an Architecture section](https://github.com/yegor256/cactoos/commit/18a0c8ad8d8b64d6bbb62fa36e668f967afda2ac) to the README of [`yegor256/cactoos`](https://github.com/yegor256/cactoos). The commit message links a Claude Code session. The new section says:

> Explicit caching requires opting in with `Sticky` or `StickyList`.

`StickyList` was [removed in 2020](https://github.com/yegor256/cactoos/commit/eed03c1fc05217eeda0564999cae2c87709b1e13). It survived in a comparison table further up the same README, an agent asked to describe the architecture read that table, and the old name came out the other side as a confident new instruction with a fresh timestamp. The next agent to open that README now has two sentences telling it to use a class that isn't there, and the newer one looks authoritative.

<div class="bp-callout bp-callout--amber">
A wrong sentence in a doc used to cost one confused engineer an afternoon. Now it is read by every agent run, acted on without question, and copied into the next document.
</div>

## The design doc got a new job

Writing the design down first is not new. Teams at Google [generally require a design document](https://abseil.io/resources/swe-book/html/ch10.html) before any significant change to a product or service. Amazon replaced the slide deck with the six-pager. The logic never needed defending: thinking is cheaper than building, and a decision that is written down can be argued with before it costs anything.

For all of that history the document had one reader, and it was a person. They read it, argued with it, went off to write the code, and noticed when something in it had stopped being true. The document shaped the result the way a conversation does: loosely, through a human who could tell when it was wrong.

That is no longer what happens. The design doc is now the thing the code is generated *from*. The practice has a name, spec-driven development, and a toolchain: GitHub's [Spec Kit](https://github.com/github/spec-kit), AWS's [Kiro](https://kiro.dev), and the `AGENTS.md` and `CLAUDE.md` files that tell an agent how the system is put together before it touches it. The pitch is that the specification stops being a forgotten page on a wiki and becomes the operational contract the system is built to.

<div class="bp-callout bp-callout--amber">
Every other artefact your code is built from has something that checks it. Source has a compiler. Tests have a runner. Types have a checker, schemas have a validator, manifests have a resolver. The document your agents build from has nothing.
</div>

That is the gap. If the doc is the guideline the code is generated from, it has to be the guideline the code is *maintained* against, on every change, by something that reads the document and the code and complains when they disagree. Otherwise it is a contract nobody enforces, and line 258 is what an unenforced contract looks like after ten months.

## The sentences that don't fail the build

A stale class name is the version of this problem you can see. The agent trips over it and recovers. The expensive version is silent.

<div class="bp-figure" data-reveal>
<p class="bp-figure-title">Two ways an agent strays from the docs</p>
<div class="bp-outcomes">
<div class="bp-outcome bp-outcome--amber"><p class="bp-outcome-name">It follows a sentence that is no longer true</p><p class="bp-outcome-desc"><em>&ldquo;Use <code>McpServerFactory</code>.&rdquo;</em> The build breaks, the agent recovers, you pay in turns and tokens. If the sentence describes a pattern rather than a class, the build doesn&rsquo;t break, and the agent reproduces a design the team retired.</p></div>
<div class="bp-outcome bp-outcome--danger"><p class="bp-outcome-name">It ignores a sentence that is still true</p><p class="bp-outcome-desc"><em>&ldquo;Controllers go through the service layer.&rdquo; &ldquo;<code>core</code> never imports from <code>plugins</code>.&rdquo;</em> The agent takes a shortcut. The code compiles, the tests pass, the diff looks fine, and the document that forbade it is not in the diff. Merged.</p></div>
</div>
</div>

The second one is where architecture goes. Not in one dramatic change but in a hundred small ones, each of which compiled, each reviewed as a diff, each contradicting a sentence nobody had open. A year later the layering the team agreed on is a description of a system that no longer exists, and every change costs more than it should.

We build [Striff](https://striff.io) to close that gap. It reads the documents already in a repository, turns each sentence that makes a claim about the code into a rule, and checks the rule against the parsed code before and after every pull request. We ran it over roughly six hundred recent public pull requests in Java, C#, TypeScript and Python. From those repositories' own documentation it read 7,161 rules:

<div class="bp-figure" data-reveal>
<p class="bp-figure-title">Every verdict, then the ones that accuse</p>
<div class="bp-zoom">
<div class="bp-zoom-strip bp-zoom-strip--all"><span class="is-held" style="flex:5519"></span><span class="is-acc" style="flex:155"></span></div>
<div class="bp-zoom-legend"><span><i class="is-held"></i>5,519 held</span><span class="bp-zoom-legend-acc"><i class="is-acc"></i>155 accuse</span></div>
<div class="bp-zoom-bracket" aria-hidden="true"><span></span></div>
<div class="bp-zoom-strip bp-zoom-strip--acc"><span class="is-viol" style="flex:97"></span><span class="is-prior" style="flex:53"></span><span class="is-rest" style="flex:5"></span></div>
<div class="bp-zoom-tiles">
<div class="bp-zoom-tile is-viol"><b>97</b><span>broken by the pull request under review</span></div>
<div class="bp-zoom-tile is-prior"><b>53</b><span>already broken before it, never blamed on the author</span></div>
<div class="bp-zoom-tile is-rest"><b>5</b><span>restored: broken before, true after</span></div>
</div>
</div>
<p class="bp-figure-caption">Roughly six hundred public pull requests; 7,161 rules read out of their repositories&rsquo; own documentation. Ninety-seven times a pull request contradicted a sentence in its own docs and would have merged without anyone comparing the two. Docs are right almost all the time, which is exactly why the exceptions get trusted. <a href="/blog/design-docs-are-enforceable-now">Here is one of the 97, sentence to verdict.</a></p>
</div>

Ninety-seven is not a firehose, and it shouldn't be: a check that fires on every pull request gets muted in a week. The point is that each of those 97 was surfaced on the line that caused it, in the pull request, while someone was already looking, instead of a year later as "how did the architecture end up like this?"

## What we found, and where

The sweep was not small repositories. Every one had over a thousand stars, and a third had over ten thousand:

<div class="bp-figure" data-reveal>
<div class="bp-stats">
<div class="bp-stat bp-stat--brand"><div class="bp-stat-value">7.6M</div><div class="bp-stat-label">GitHub stars across the repositories scanned</div></div>
<div class="bp-stat bp-stat--brand"><div class="bp-stat-value">175</div><div class="bp-stat-label">of them with more than 10,000 stars</div></div>
<div class="bp-stat bp-stat--amber"><div class="bp-stat-value">7,161</div><div class="bp-stat-label">rules read out of their own documentation and checked at both revisions</div></div>
<div class="bp-stat bp-stat--danger"><div class="bp-stat-value">97</div><div class="bp-stat-label">pull requests told, before merge, that they contradicted their own docs</div></div>
</div>
</div>

These are the findings we checked by hand and can stand behind, largest repository first. Each links the sentence and the evidence.

<div class="bp-figure" data-reveal>
<p class="bp-figure-title">Checked by hand, largest first</p>
<div class="bp-find">
<div class="bp-find-row"><span class="bp-find-repo"><b>apache/dolphinscheduler</b><i>14.5k &#9733;</i></span><span class="bp-find-body"><span class="bp-find-doc"><a href="https://github.com/apache/dolphinscheduler/blob/dev/docs/docs/en/contribute/backend/spi/registry.md?plain=1#L20">Contributor guide</a> says a new registry plugin &ldquo;only needs to implement <code>RegistryFactory</code>&rdquo;.</span><span class="bp-find-code">The interface was <a href="https://github.com/apache/dolphinscheduler/commit/67cc260d52c1f2e5aa7db76aa8621cdd0f8c4ee0">removed in a package split</a>. First-time contributors, and agents, are sent to implement something that isn&rsquo;t there.</span></span><span class="bp-find-tag is-absent">type gone</span></div>
<div class="bp-find-row"><span class="bp-find-repo"><b>aspnetboilerplate</b><i>12.0k &#9733;</i></span><span class="bp-find-body"><span class="bp-find-doc"><a href="https://github.com/aspnetboilerplate/aspnetboilerplate/blob/dev/doc/WebSite/Startup-Configuration.md?plain=1#L62">Startup-Configuration.md</a> walks the reader through <code>IAbpModuleConfigurations</code>.</span><span class="bp-find-code">No such type in the code; it was <a href="https://github.com/aspnetboilerplate/aspnetboilerplate/commit/d46b89df692fdb5e6caa7b06785fb6d49dea5978">replaced</a> and the page never followed.</span></span><span class="bp-find-tag is-absent">type gone</span></div>
<div class="bp-find-row"><span class="bp-find-repo"><b>dotnet/BenchmarkDotNet</b><i>11.5k &#9733;</i></span><span class="bp-find-body"><span class="bp-find-doc"><a href="https://github.com/dotnet/BenchmarkDotNet/blob/master/docs/articles/configs/exporters.md?plain=1#L101">exporters.md</a> documents four properties of <code>ISummaryStyle</code>, with defaults.</span><span class="bp-find-code">The interface was <a href="https://github.com/dotnet/BenchmarkDotNet/commit/f10752ace1bf4c5e4316c6f9b7b34ca25ac46c19">deleted</a>; the properties live on a class with a different name.</span></span><span class="bp-find-tag is-absent">type gone</span></div>
<div class="bp-find-row"><span class="bp-find-repo"><b>spring-projects/spring-ai</b><i>9.5k &#9733;</i></span><span class="bp-find-body"><span class="bp-find-doc"><a href="https://github.com/spring-projects/spring-ai/blob/main/spring-ai-docs/src/main/antora/modules/ROOT/pages/api/audio/speech/openai-speech.adoc">The speech reference</a> builds its whole walkthrough on <code>OpenAiAudioApi</code>, about twenty mentions.</span><span class="bp-find-code">The project <a href="https://github.com/spring-projects/spring-ai/commit/75fa844869786f80113bef7a4a557e9600452ba6">moved to the official OpenAI Java SDK</a> five months ago. An agent asked to add speech reads twenty sentences describing the design that was replaced.</span></span><span class="bp-find-tag is-absent">type gone</span></div>
<div class="bp-find-row"><span class="bp-find-repo"><b>apache/pinot</b><i>6.1k &#9733;</i></span><span class="bp-find-body"><span class="bp-find-doc"><code>pinot-sql-ddl/DESIGN.md</code> says the module &ldquo;depends only on <code>pinot-spi</code>, <code>pinot-common</code>, and <code>calcite-babel</code>&rdquo;.</span><span class="bp-find-code">It also depends on <code>pinot-query-planner</code>: declared in its pom, imported by <code>MaterializedViewSchemaInferer</code>. The design doc describes a boundary the code crossed.</span></span><span class="bp-find-tag is-prior">rule broken</span></div>
<div class="bp-find-row"><span class="bp-find-repo"><b>modelcontextprotocol/csharp-sdk</b><i>4.5k &#9733;</i></span><span class="bp-find-body"><span class="bp-find-doc"><a href="https://github.com/modelcontextprotocol/csharp-sdk/blob/main/.github/copilot-instructions.md?plain=1#L258">copilot-instructions.md:258</a> tells the agent to use <code>McpServerFactory</code>.</span><span class="bp-find-code">Written by Copilot, <a href="https://github.com/modelcontextprotocol/csharp-sdk/commit/136755f2be0c78193a0b0e82298e9eaa9f424794">deleted by Copilot</a>. The only remaining mention is the instruction.</span></span><span class="bp-find-tag is-absent">agent file</span></div>
<div class="bp-find-row"><span class="bp-find-repo"><b>MonoGame.Extended</b><i>1.8k &#9733;</i></span><span class="bp-find-body"><span class="bp-find-doc"><code>CODING_GUIDELINES.md</code>: &ldquo;Helper classes should be static classes with static methods.&rdquo;</span><span class="bp-find-code">Six of the seven <code>*Helper</code> types are static. <code>PrimitivesHelper</code> is an <code>internal class</code>. A rule over every class of a kind, and the one exception found.</span></span><span class="bp-find-tag is-prior">rule broken</span></div>
<div class="bp-find-row"><span class="bp-find-repo"><b>yegor256/cactoos</b><i>782 &#9733;</i></span><span class="bp-find-body"><span class="bp-find-doc"><a href="https://github.com/yegor256/cactoos/blob/master/README.md?plain=1#L404">README.md:404</a>, an Architecture section written by an agent in 2026, says caching needs <code>StickyList</code>.</span><span class="bp-find-code">The class was <a href="https://github.com/yegor256/cactoos/commit/eed03c1fc05217eeda0564999cae2c87709b1e13">removed in 2020</a>. The agent copied it from a table further up the same README.</span></span><span class="bp-find-tag is-absent">agent-written</span></div>
<div class="bp-find-row"><span class="bp-find-repo"><b>Ericsson/ecchronos</b><i>PR #1786</i></span><span class="bp-find-body"><span class="bp-find-doc"><code>core.impl/README.md:136</code>: <code>NodeWorker</code> &ldquo;calls <code>RepairScheduler.putConfigurations()</code>&rdquo;.</span><span class="bp-find-code">True before the pull request, false after it: a refactor moved the call into a new class and the README was not in the diff. Caught on the change that did it.</span></span><span class="bp-find-tag is-viol">broken by the PR</span></div>
</div>
<p class="bp-figure-caption">Star counts and default branches as of 24 September 2026. &ldquo;Rule broken&rdquo; means the documented rule fails in the code at both revisions of the pull request checked; &ldquo;broken by the PR&rdquo; means it held before and fails after. <a href="/blog/design-docs-are-enforceable-now#a-real-example">The ecChronos case, end to end.</a></p>
</div>

## Why nothing you already run catches this

Every check in a pipeline compares code to code. Tests execute it, types constrain it, the linter parses it, the dependency audit reads a manifest. A sentence in a markdown file is in none of those places. It is not compiled, not executed, and not in the diff, so no reviewer has a reason to open it and no tool has an opinion about it.

<div class="bp-callout">
The sentence and the code that contradicts it are both plainly visible. They are just never visible to the same reader at the same moment.
</div>

None of this is hard to check. "`core` must not import from `plugins`" is a one-line question about a dependency graph, and your compiler answers harder questions on every build. What was missing is anything that turns the sentence into the question.

## What to do about it

- **Treat your agent instruction files as code.** They are an input to your codebase now. When you delete or rename a type, `git grep -w OldName -- '*.md'` takes a second, and IDE rename refactorings skip markdown.
- **Keep them short, and prefer rules to names.** "Controllers never call repositories directly" stays true across a hundred refactors. A class name is a claim that can go stale on the next one.
- **Have the docs checked on the pull request**, where the change that contradicts them is being reviewed. This is what the MCP finding looks like when Striff posts it:

<div class="bp-figure" data-reveal>
<div class="bp-check">
<div class="bp-check-bar"><img src="/icon.svg" alt="" width="18" height="18" /><span class="bp-check-name"><b>Striff</b> / architectural-review</span><span class="bp-check-state">Documented types &middot; 1 finding</span></div>
<div class="bp-check-item">
<span class="bp-check-dot" aria-hidden="true"></span>
<div class="bp-check-text">
<p class="bp-check-title">.github/copilot-instructions.md names 1 type this repository does not have</p>
<p class="bp-check-summary"><code>.github/copilot-instructions.md</code> writes <code>McpServerFactory</code>. This revision declares no type by that name. This is a claim about the page rather than about this change: no diff took part in it, and it may have been wrong for a long time.</p>
<div class="bp-check-ev"><span class="bp-check-ev-k">evidence</span><span>this repository once held <code>src/ModelContextProtocol.Core/Server/McpServerFactory.cs</code> and does not now; .github/copilot-instructions.md says: &ldquo;Use <code>McpServerFactory</code> to create server instances with configured options&rdquo;</span></div>
</div>
</div>
</div>
<p class="bp-figure-caption">Posted on the pull request beside the tests, citing the line and the commit that removed the type. The architectural rules in the same documents are checked the same way, and a change that breaks one is told so before it merges.</p>
</div>

Striff runs as a [GitHub App](https://striff.io), free on public repositories. It reads the documents already in the repository, so there is nothing to write and nothing to configure: the rules are the ones your team already wrote down, and the agents are already reading them.

<div class="bp-note">
<p class="bp-note-title">Method notes</p>
<p>Roughly six hundred recent public pull requests across several hundred repositories in Java, C#, TypeScript and Python. Code is parsed from source without compiling it, so the model has no generated members and no resolved annotations; where that leaves a rule unanswerable, the check reports nothing rather than a pass or a failure. A documented type counts as absent only on an exact-token match against the whole revision, with the repository&rsquo;s history checked to tell a moved type from a removed one. Every repository named above was checked by hand against its default branch in September 2026; line numbers are for the default branch at the time of writing.</p>
</div>
